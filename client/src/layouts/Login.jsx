import React from 'react';
import Page from '../components/Page';
import { Button } from '@material-ui/core';

const Login = () => {
  return (
    <Page>
      <Button variant="contained" color="secondary">BOI</Button> 
      <div>Login</div>
    </Page>
  );
};

export default Login;
